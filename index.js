var recipes = {breakfast: 'oatmeal'};

function updateObjectWithKeyAndValue(object, key, value){
   
updateObjectWithKeyAndValue.assign({}, obj, { [key]: value });

   return object[key] = 'value' ;
}
